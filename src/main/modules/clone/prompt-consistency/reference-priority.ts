export function generateReferencePriorityRules(referencePriorityMode: 'standard' | 'hard') {
  if (referencePriorityMode === 'hard') {
    return 'REFERENCE IMAGE PRIORITY: The reference images define the product identity only. They override all textual descriptions only for product structure, silhouette, geometry, proportions, scale, component count, material response, reflection behavior, attachment logic, and accessory category. Any human shown in the product reference images is invalid and must be ignored completely. Never copy model identity, face, hair, skin tone, ear shape, body, or outfit from product references. If any conflict occurs, follow the selected model identity for the person and follow the reference images for the product. If any cinematic instruction conflicts with product identity, always follow the reference images. Cinematic treatment must never override identity.'
  }
  return 'REFERENCE IMAGE PRIORITY: Keep the generated product aligned to the uploaded reference images and preserve the same silhouette, structure, proportions, scale, component count, and product category. Treat any person appearing in the product reference images as invalid and ignore that person completely; only the selected model identity may define the human.'
}
