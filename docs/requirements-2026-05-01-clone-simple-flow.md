# 2026-05-01 /clone 简化流程（新手模式）

## 目标
- 将复刻工坊改为“傻瓜式分步操作”。
- 每一步只保留一个主动作，减少同屏复杂配置。

## 本次改动
- `/clone` 新增“新手分步模式”（默认开启）：
  - 顶部新增新手引导卡，显示当前步骤主按钮。
  - 支持在“新手模式 / 专业模式”之间切换。
- 新手模式下：
  - 优先按步骤自动引导主动作（分析、变体、评分、方案、出片）。
  - 隐藏手动 AI 首尾帧和高级设置折叠区，避免干扰。
- 仍保持原有 `/clone` 数据结构与主进程链路不变，兼容旧项目。

## 新手模式建议流程
1. 上传并分析爆款  
2. 确认产品信息并进入分镜  
3. 生成分镜变体  
4. AI评分筛选  
5. 生成视频方案  
6. 生成 Top 3 视频  
7. 预览与批量出片  
