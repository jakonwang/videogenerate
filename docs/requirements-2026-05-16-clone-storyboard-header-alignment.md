# 2026-05-16 Clone 分镜设计头部对齐

## 目标

- 将 `/clone/[projectId]` 中“分镜设计”阶段的标题、描述和右侧按钮对齐到设计稿结构。

## 本轮最小改动

- 修改文件：
  - `src/renderer/src/ui/views/CloneView.vue`

- 具体调整：
  - 不修改共享 `CloneStageHeader` 组件。
  - 仅在分镜设计阶段局部替换为专用头部结构。
  - 左侧显示：
    - 标题：`分镜设计`
    - 描述：`基于脚本内容和风格，AI 为你生成分镜画面，支持调整镜头、画面和提示词`
  - 右侧显示：
    - `保存项目`
    - `下一步`
  - 行为保持最小闭环：
    - 有分镜图时，`下一步` 进入分镜视频阶段
    - 无分镜图时，主按钮继续执行分镜生成

## 结果

- 分镜设计阶段头部更接近用户设计稿。
- 改动范围仅限当前阶段，不影响其他阶段标题样式。

## 验证

- Windows 开发环境执行：
  - `npm run typecheck`
