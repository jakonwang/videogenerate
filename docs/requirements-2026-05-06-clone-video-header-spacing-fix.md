# 2026-05-06 `/clone` 分镜视频阶段头部排版修复

## 背景

`/clone` 第 4 阶段“分镜生成视频”顶部标题、描述与主按钮之间的层级不明显，通用头部组件的小幅调整在页面上不够可感知，用户反馈“没有反应”。

## 修改范围

- `D:\phpstudy_pro\WWW\videogenerate\src\renderer\src\ui\views\CloneView.vue`

## 本次调整

1. 对第 4 阶段头部增加页面级专属类名，不再只依赖通用组件样式。
2. 给头部补充一行小型辅助状态：
   - 当前分镜数量
   - 失败分镜数量
   - 当前项目名称
3. 对标题区、描述区、按钮区做页面级强制间距和容器样式覆盖，确保：
   - 标题与描述之间有清晰节奏
   - 主按钮与左侧文案之间有明显分隔
   - 辅助状态行与下方工作区之间视觉分层更稳定

## 验证

执行：

- `npm run typecheck`
- `npm run build`

预期：

- 第 4 阶段头部的标题、描述、按钮、辅助状态排版明显改善
- 不影响其它阶段头部样式
