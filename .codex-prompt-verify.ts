import { sanitizeGeneratedVideoPrompt } from './src/main/modules/clone/prompt'

const dirty = String.raw`remium realistic social commerce video, shot on a modern smartphone camera, natural daylight, clean editorial composition, real human hands, real shadows, real lens perspective, believable e-commerce demo. Hard-copy the reference shot action form, background category, camera distance, composition, hand gesture and motion path. Replace only the person identity and product identity. Do not copy the original person, watermark, captions, stickers or platform UI. Avoid CGI, 3D render, plastic toy look, fantasy scene, over-smoothed skin, warped fingers, fake text, watermark, subtitles, captions, UI overlays, account names, stickers and logos. Shot script lock: preserve this specific shot logic. Script role: unknown. Script text: Model usage demonstration shot. Preserve body/hand action rhythm and replace product/model identity.. tight detail reveal with subtle hand interaction. zoom_in 前3秒直接抛出痛点与结果反差，语气更像真实带货口播。. Visual description: 真实生活场景中的人物带货镜头，Model usage demonstration shot. Preserve body/hand action rhythm and replace product/model identity.. Action description: 人物自然演示产品并快速给出结果反馈，tight detail reveal with subtle hand interaction. Camera description: 手机手持近景快速切入，extreme_closeup framing, zoom_in. Product focus: keep the product visible and consistent with the reference selling logic. Generation prompt: 真实生活场景中的人物带货镜头，Model usage demonstration shot. Preserve body/hand action rhythm and replace product/model identity. 人物自然演示产品并快速给出结果反馈，tight detail reveal with subtle hand interaction 手机手持近景快速切入，extreme_closeup framing, zoom_in Product focus: keep the product visible and consistent with the reference selling logic. Commercial short-video realism, no watermark, no subtitles, no UI, no logo, 9:16 vertical frame.. Start-frame constraint: first frame must already match the shot script role, scene, product placement and action setup. End-frame constraint: last frame must naturally complete the same script beat without changing scene, action type, product or person identity. Script confidence: 0.00. Analysis notes: 脚本分析失败：未启用聚合接口对话模型. Script negative: watermark, account name, platform UI, copied face identity, subtitles, logo mismatch. Reference lock mode: hard_reference_motion. Scene/background: same close product-demo background category as the reference shot. Subject pose: keep the same hand/product position and product-to-camera distance as the reference shot. Product action: tight detail reveal with subtle hand interaction. Camera composition: extreme_closeup framing; preserve product screen position, subject crop, camera distance and product size ratio from the reference shot. Motion path: slow push-in following the same reference movement path. Must preserve: background category and atmosphere: same close product-demo background category as the reference shot; subject pose and hand placement: keep the same hand/product position and product-to-camera distance as the reference shot; product demonstration action: tight detail reveal with subtle hand interaction; camera composition: extreme_closeup framing; preserve product screen position, subject crop, camera distance and product size ratio from the reference shot; motion path and rhythm: slow push-i`

const clean = sanitizeGeneratedVideoPrompt(dirty, 1800)
console.log(clean)
console.log('\n---CHECK---')
console.log(JSON.stringify({
  hasCjk: /[\u3400-\u9fff\u3040-\u30ff\uac00-\ud7af]/.test(clean),
  hasScriptConfidence: /script confidence/i.test(clean),
  hasAnalysisNotes: /analysis notes/i.test(clean),
  hasReferenceLockMode: /reference lock mode/i.test(clean),
  hasMustPreserve: /must preserve/i.test(clean),
  hasShotScriptLock: /shot script lock/i.test(clean),
  hasScriptRole: /script role:/i.test(clean),
  hasGenerationPromptLabel: /generation prompt:/i.test(clean),
  hasSlowPushBroken: /slow push-i$/i.test(clean),
  length: clean.length,
}, null, 2))
