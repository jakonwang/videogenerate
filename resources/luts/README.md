## 3D LUT（.cube）目录

将你的 `.cube` 文件放到本目录，应用启动后会自动读取文件列表，并在「模板 → 画面调色 → 风格滤镜」下拉框中出现。

### 说明

- **文件格式**：仅识别 `.cube`
- **命名建议**：用英文/数字/下划线，避免特殊字符（更稳）
- **部署**：打包后 LUT 会被放入 `out/main/resources/luts/`（开发时读取项目根的 `resources/luts/`）

