# AGENTS.md

在涉及 UI、页面、组件、布局、样式或设计稿转代码的任务时，必须优先使用 Stitch MCP。

工作流程必须遵循：

1. 先通过 Stitch MCP 查看项目中的设计页面（screen）
2. 获取目标页面的代码或截图
3. 根据设计稿，在当前项目中实现页面
4. 不允许直接把生成的 HTML 原样粘贴进生产代码（除非明确要求）
5. 必须按照当前项目的组件结构、框架规范（React/Vue/Next等）重写
6. 尽量复用已有组件
7. 保持设计稿的布局、间距、颜色、层级一致
8. 完成后运行项目并验证页面效果

如果没有先调用 Stitch MCP，不允许直接开始写代码